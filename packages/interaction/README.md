# raw2d-interaction

Optional interaction helpers for Raw2D.

This package is intentionally separate from `raw2d-core` and renderers. It owns editor-style pointer workflows such as dragging, selection helpers, and future transform tools.
