import type { TextureSize, TextureSource } from "./Texture.type.js";

export interface TextureSourceSizeOptions {
  readonly source: TextureSource;
}

export interface TextureSourceSize extends TextureSize {}

