# Raw2D v1.19.5 - Studio Import Export Phase

Raw2D v1.19.5 publishes the Studio import/export polish phase.

## Added

- Studio import validation messages for unsupported objects, invalid geometry, and missing asset references.
- Canvas code export that copies public `raw2d` app code without importing Studio internals.
- WebGL code export with explicit WebGL2 checks, texture placeholder warnings, and renderer diagnostics.
- Raw2D MCP scene JSON import with deterministic handling for valid, duplicate, and invalid object IDs.
- English and Hinglish docs for Studio import/export, validation, generated-code boundaries, and MCP import behavior.
- Browser and unit coverage for Studio generated code, MCP import, persistence wiring, and docs route coverage.

## Notes

- Studio generated snippets remain app code, not Studio save files.
- Canvas export is the full-fidelity generated-code path. WebGL export is explicit about renderer support limits.
- MCP import repairs duplicate and invalid IDs deterministically and reports each repair as a warning.
- Package versions are aligned at `1.19.5` across every published Raw2D package.
- npm publish and Cloudflare deployment should still be performed by the release workflow or maintainer with credentials.

## Verification

- `npm test`
- `npm run build:docs`
- `node --test tests/studio/load.test.mjs tests/studio/canvas-code-export.test.mjs tests/studio/webgl-code-export.test.mjs tests/browser/studio-code-export.test.mjs tests/browser/studio-persistence.test.mjs`
- `npm run pack:check -- --silent`
- `npm run test:cdn:pinned`
- `git diff --check`
