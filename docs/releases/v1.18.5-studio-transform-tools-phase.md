# Raw2D v1.18.5 - Studio Transform Tools Phase

Raw2D v1.18.5 publishes the Studio transform tools phase.

## Added

- Studio Circle resize handles that update center and radius without storing negative radius.
- Studio Line endpoint handles for editing local start and end points.
- Studio Text2D resize behavior that scales `px` font size from estimated text bounds.
- Studio multi-select state, layer shift-select, group selection bounds, group move, and group delete workflows.
- Batch Studio commands so grouped transform/delete edits stay atomic for undo and redo.
- Browser regression coverage for resize handles crossing all four corners.
- English and Hinglish docs for transform tool behavior and multi-select movement.

## Notes

- Studio transform tools stay inside `apps/studio`; runtime renderer packages still receive scene state through the adapter.
- Text resize intentionally updates font size instead of adding width or height fields to `Text2D`.
- Multi-select movement updates object `x` and `y` only, keeping object geometry explicit and debuggable.
- Package versions are aligned at `1.18.5` across every published Raw2D package.

## Verification

- `npm test`
- `npm run build:docs`
- `node --test tests/browser/studio-line-resize.test.mjs tests/browser/studio-text-resize.test.mjs tests/browser/studio-resize-visual.test.mjs tests/browser/studio-undo-redo.test.mjs`
- `npm run pack:check -- --silent`
- `npm run test:cdn:pinned`
- `git diff --check`
