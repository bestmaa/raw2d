# Raw2D v1.20.5 - Effects Foundation Phase

Raw2D v1.20.5 publishes the effects foundation phase.

## Added

- `raw2d-effects` descriptor factories and validation helpers for opacity, blur, grayscale, and shadow.
- Canvas effect application through explicit renderer hooks while keeping scene objects renderer-neutral.
- WebGL effect pass planning that separates draw-batch alpha work from framebuffer shader-pass work.
- `/examples/effects-basic/` showing Canvas-first effect behavior and WebGL support limits.
- Effects package docs covering APIs, non-goals, renderer boundaries, and core separation.

## Notes

- `raw2d-effects` owns plain data only. Canvas and WebGL packages own execution details.
- WebGL effect execution is not complete yet; the current public API exposes pass planning and support limits.
- Package versions are aligned at `1.20.5` across every published Raw2D package.
- npm publish and Cloudflare deployment should still be performed by the release workflow or maintainer with credentials.

## Verification

- `npm test`
- `npm run build:docs`
- `npm run pack:check -- --silent`
- `node --test tests/effects/effects-descriptors.test.mjs tests/canvas/effects.test.mjs tests/webgl/effects-boundary.test.mjs tests/examples/effects-basic.test.mjs tests/docs/effects-package.test.mjs`
- `git diff --check`
